import React from 'react';
import { Link } from 'react-router-dom';
// We will add the CSS import later

const NavBar = () => {
  return (
    <nav className="navbar" style={{backgroundColor: '#3f51b5', padding: '10px', color: 'white'}}>
      <h1>Community Task Manager</h1>
      <div className="nav-links">
        {/* Link switches pages with no reloading */}
        <Link to="/todos" style={{color: 'white', marginRight: '15px'}}>Tasks</Link>
        <Link to="/contact" style={{color: 'white'}}>Contact</Link>
      </div>
    </nav>
  );
};

export default NavBar;